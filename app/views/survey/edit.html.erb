 
 

 <div class="Survey new">
 	
 	<!-- Hey Mike I got rid of this parameter in the form_for call :
 		:url => {:action => 'update', :id => @survey.id}
 		
 		Instead I added the paramter @survey -->
		<%= form_for(:survey, :url => {:action => 'update', :id => @survey.id}) do |f|%>
		
	<table summary="Survey form fields" class="center fixedWidth" >
		<tr>
			
			<th style="border:none;">Survey Title </th>
		</tr>
		<tr>
			<td ><%= f.text_field(:title) %>  &nbsp;&nbsp;&nbsp;&nbsp;  <%= submit_tag("Edit Survey Title") %></td>
			 
			
		</tr>
	<br />
		 
	
	</table>
<%end%>	 
	 <table class="center">
	 	<tr>
	 		<td>	
<table  summary="Questions List">
 <tr class="header">
  <th>ID</th>
  <th>Questions</th>
  <th>Action</th>
</tr>
<% num=0%>
<% @survey.questions.each do |t| %>
<tr>
	<td><%=num= num +1 %></td>
	<td ><%= t.title %></td>
	
	
	<td class="center">
		<center>
		<table class="hover" class="center">
		<tr><td ><%= link_to("Edit",  {:controller => 'question', :action =>'edit', :id => t.id}, :class => 'action edit')   %></td></tr>
		<tr><td ><%= link_to("Delete", {:controller => 'question', :action =>'delete', :id => t.id}, :class => 'action delete')  %></td></tr>
	</table>
		</center>
	</td>
 
	
	 
</tr>

<% end %>

	
	</table>
	</td>
	</tr>
		<tr>
			<td style="background-color: #d0e4fe; padding:0; margin:0;">
				<center>
				<h4 ><%=link_to("Add a Question", {:controller=>'question', :action =>'new', :id => @survey.id}, :class => "new_box")%></h4>
				</center>
			</td>
		</tr>
	</table>
</div>


	
